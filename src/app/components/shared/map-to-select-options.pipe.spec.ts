import { MapToSelectOptionsPipe } from './map-to-select-options.pipe';

describe('MapToSelectOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new MapToSelectOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
